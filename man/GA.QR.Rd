% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantGA.R
\name{GA.QR}
\alias{GA.QR}
\title{Fits a linear quantile regression model using a genetic algorithm.}
\usage{
GA.QR(
  formula = NULL,
  data = NULL,
  X = NULL,
  y = NULL,
  tau = 0.5,
  intercept = TRUE,
  pop_size = 50,
  max_generations = 200,
  crossover_prob = 0.8,
  mutation_prob = 0.1,
  mutation_strength = 0.5,
  bounds = list(lower = -100, upper = 100),
  tournament_size = 3,
  elitism_rate = 0.1,
  convergence_tolerance = 1e-06,
  convergence_generations = 50,
  seed = NULL
)
}
\arguments{
\item{formula}{\code{formula}\cr Model formula specifying the response and
predictors. If supplied, \code{X} and \code{y} are ignored. \code{NULL} by
default.}

\item{data}{\code{data.frame}\cr Data in which to evaluate the formula.
Only used when \code{formula} is provided.}

\item{X}{\code{matrix}\cr Design matrix for the regression. Defaults to
\code{NULL} and will be created from \code{formula} when provided.}

\item{y}{\code{numeric}\cr Response vector. Defaults to \code{NULL}.}

\item{tau}{\code{numeric}\cr Quantile to be modelled (between 0 and 1).
Defaults to \code{0.5}.}

\item{intercept}{\code{logical}\cr Should an intercept be included when a
formula is supplied? Defaults to \code{TRUE}.}

\item{pop_size}{\code{integer}\cr Size of the population in the genetic
algorithm. Defaults to \code{50}.}

\item{max_generations}{\code{integer}\cr Maximum number of generations to
evolve. Defaults to \code{200}.}

\item{crossover_prob}{\code{numeric}\cr Probability of crossover between two
parents. Defaults to \code{0.8}.}

\item{mutation_prob}{\code{numeric}\cr Probability that a coefficient is
mutated. Defaults to \code{0.1}.}

\item{mutation_strength}{\code{numeric}\cr Standard deviation of the
Gaussian mutation. Defaults to \code{0.5}.}

\item{bounds}{\code{list}\cr List with elements \code{lower} and
\code{upper} giving numeric bounds for the coefficients. Defaults to
\code{list(lower = -100, upper = 100)}.}

\item{tournament_size}{\code{integer}\cr Number of individuals taking part in
tournament selection. Defaults to \code{3}.}

\item{elitism_rate}{\code{numeric}\cr Proportion of the best individuals
carried over to the next generation. Defaults to \code{0.1}.}

\item{convergence_tolerance}{\code{numeric}\cr Not currently used. Intended
relative tolerance for convergence. Defaults to \code{1e-6}.}

\item{convergence_generations}{\code{integer}\cr Number of successive
generations without improvement before stopping. Defaults to \code{50}.}

\item{seed}{\code{numeric}\cr Optional seed for reproducibility. Defaults to
\code{NULL}.}
}
\value{
A list of class \code{quantileGA} containing the fitted coefficients,
standard errors, fitted values, residuals and diagnostics.
}
\description{
Fits a linear quantile regression model using a genetic algorithm.
}
\examples{
set.seed(45)
n <- 100
x1 <- rnorm(n)
x2 <- runif(n, -1, 1)
beta_true <- c(2, -1, 0.5)
y <- as.numeric(cbind(1, x1, x2) \%*\% beta_true + rt(n, df = 3))
df <- data.frame(y = y, x1 = x1, x2 = x2)

result <- GA.QR(
  y ~ x1 + x2,
  data = df,
  tau = 0.5,
  pop_size = 50,
  max_generations = 100,
  tournament_size = 10,
  elitism_rate = 0.5,
  mutation_prob = 0.5,
  mutation_strength = 0.9,
  convergence_generations = 20,
  seed = 42
)
summary(result)
}
